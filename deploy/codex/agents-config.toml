# Merge this block into the Codex user's ~/.codex/config.toml. Standalone
# custom roles are discovered from ~/.codex/agents/*.toml by their name field.
[agents]
enabled = true
max_concurrent_threads_per_session = 2
default_subagent_model = "gpt-5.6-luna"
default_subagent_reasoning_effort = "low"
